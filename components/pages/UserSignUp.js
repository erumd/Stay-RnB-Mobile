import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

export const UserSignUp = (props) => {
  return (
    <View>
      <Text>This is the user sign up page!</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

